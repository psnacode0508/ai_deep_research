feat: build research workspace and API contract

- Replaced the placeholder `/research/new` with a polished research configuration form supporting Research Depth and Advanced settings (domains, source types, approvals).
- Created the `/research/:id` workspace dashboard, featuring a Left Panel (Research Plan), Center Panel (Live Engine Activity), Right Panel (Sources), and Header.
- Updated `DashboardPage` to fetch and display actual research sessions.
- Defined backend API structure for `/api/v1/research` (POST, GET, DELETE) integrated directly with the Supabase schema, including RLS checking on the backend where applicable via explicit user queries.
- Refined `packages/shared` types (`ResearchSessionMetadata`, `CreateResearchRequest`, updated `ResearchSession` interface).
- Established the `ResearchEngine` boundary stub to decouple the API from future LangGraph AI integration.
