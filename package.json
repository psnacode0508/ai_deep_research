{
  "name": "deepresearch",
  "version": "0.0.1",
  "private": true,
  "description": "Multi-Agent AI Research & Web Intelligence Platform",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:server": "npm run dev --workspace=apps/server",
    "build:web": "npm run build --workspace=apps/web",
    "build:server": "npm run build --workspace=apps/server",
    "type-check": "npm run type-check --workspace=packages/shared && npm run type-check --workspace=apps/server && npm run type-check --workspace=apps/web",
    "clean": "npx rimraf apps/*/dist apps/*/node_modules packages/*/dist packages/*/node_modules node_modules"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "keywords": [
    "ai",
    "research",
    "multi-agent",
    "langgraph",
    "gemini"
  ],
  "author": "",
  "license": "MIT",
  "allowScripts": {
    "esbuild@0.25.12": true
  },
  "devDependencies": {
    "@types/node": "^26.6.1"
  }
}
